import React from 'react';

const EmailLink: React.FC = () => (
  <div className="inline-container">
    <a href="mailto:hwchen@cs.utexas.edu">hwchen[at]cs[dot]utexas[dot]edu</a>
  </div>
);

export default EmailLink;
